<div class="task-editor">
  <ul>
    <li>
      <% if @user.id == true %>
          <%= @task.title %>
          <%= @task.content %>
      <% else %>
        <% "Permission Denied" %>
      <% end %>
    </li>
  </ul>
</div>
